# GH-470

**Created:** 2026-07-26

## Problem

In `cmd/server/main.go:153,229-230`, replace the three nil placeholders with the concrete services from subtask 3 (manual DI per convention). Add a testcontainers-based integration test covering the full happy path (authorize → login accept → consent accept → code → token → userinfo) plus negative cases: reused code rejected, wrong verifier rejected, `plain` challenge rejected at authorize, unregistered redirect_uri returns error (not redirect), confidential client secret path works. Verify the pre-existing handler tests (`oidc_handlers_test.go`, `admin_consent_handlers_test.go`) still pass unchanged. Update `api/` OpenAPI spec and `deployments/README.md` with the new env vars (`OIDC_LOGIN_UI_URL`, consent UI URL) and the `OIDC_ISSUER_URL` behavior-change note. Confirm `curl /.well-known/openid-configuration` returns `issuer` matching `iss` in issued tokens against a real running instance.

## Acceptance Criteria

