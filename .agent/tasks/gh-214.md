# GH-214

**Created:** 2026-04-05

## Problem

GitHub Issue #214: API integration, config, and DI wiring (`internal/api/`, `internal/config/`, ...

Parent: GH-19

Wire everything together. Add DPoP config to `internal/config/` (enabled flag, nonce TTL, jti window). Update `internal/api/token_handlers.go` to extract the `DPoP` header from token requests, call dpop proof validation, and pass the JWK thumbprint to the token service for binding. Update `api.Services` / `api.MiddlewareStack` if new interfaces are needed. Wire the DPoP service (with Redis) and DPoP middleware into `cmd/server/main.go` DI setup, inserting `DPoPMiddleware` after `AuthMiddleware` in the middleware chain. Add integration-level tests: full flow from DPoP proof at `/auth/token` through bound token usage on protected endpoints.

## Acceptance Criteria

