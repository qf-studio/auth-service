# GH-51

**Created:** 2026-04-02

## Problem

Create `router.go` with `NewPublicRouter(services, middleware) *gin.Engine` factory, middleware stack ordering (correlation ID → security headers → rate limit → request size → routes), public route group (`/auth/register`, `/auth/login`, `/auth/token`, `/auth/revoke`, `/auth/password/reset`, `/auth/password/reset/confirm`, `/.well-known/jwks.json`, health probes) and protected route group with auth middleware (`/auth/me`, `/auth/me/password`, `/auth/logout`, `/auth/logout/all`). Create `auth_handlers.go` (register, login, password reset/confirm, me, password change, logout handlers) and `token_handlers.go` (unified `/auth/token` dispatching on `grant_type`, refresh, revoke, JWKS). Each handler extracts the validated request from context, calls the appropriate service, and returns a structured response using the middleware error helpers. Create integration tests (`*_test.go`) using `gin.TestMode` + `httptest` covering: validation error response format, all endpoint status codes, and the full auth flow (register → login → refresh → logout).

## Acceptance Criteria

