# GH-332

**Created:** 2026-04-06

## Problem

Create the credential vault database schema (`migrations/000010_credential_vault.up.sql`, `migrations/000010_credential_vault.down.sql`) with tables for encrypted credential storage (credential ID, owner client ID, target name, encrypted blob, credential type, scopes, rotation metadata). Add the `AgentCredential` domain struct and constants to `internal/domain/agent_credential.go`, including credential types (API key, OAuth token, certificate), status enum, and helper methods.

## Acceptance Criteria

