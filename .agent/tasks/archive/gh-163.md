# GH-163

**Created:** 2026-04-03

## Problem

add benchmark test (`internal/config/config_test.go`) - The config struct, `Load()` function, required-var validation, defaults, and all table-driven tests are already implemented and comprehensive. The only missing piece from the issue's "Done" checklist is a benchmark for config loading. Add `BenchmarkLoad` to `config_test.go`.

## Acceptance Criteria

