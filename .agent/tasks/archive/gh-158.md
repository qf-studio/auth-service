# GH-158

**Created:** 2026-04-03

## Problem

Create `docker/Dockerfile` with multi-stage build (Go 1.24-alpine builder → alpine:3.21 runtime, non-root `appuser:1000`, CGO_ENABLED=0, expose 4000+4001). Create root `docker-compose.yml` for local development with PostgreSQL 16-alpine (healthcheck, named volume), Redis 7.4-alpine (healthcheck, named volume), and auth-service (depends_on with condition: service_healthy). Create `.env.example` documenting all expected environment variables (ports, DB, Redis, JWT, rate limits, CORS).

## Acceptance Criteria

