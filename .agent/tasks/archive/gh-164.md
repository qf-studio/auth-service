# GH-164

**Created:** 2026-04-03

## Problem

singleton + environment-based config + tests (`internal/logger/logger.go`, `internal/logger/logger_test.go`) - Rework `logger.go` from the current factory pattern to a singleton with `Init(environment string)` that switches between production mode (JSON encoder, InfoLevel, ISO8601 timestamps) and development mode (console encoder, DebugLevel). Add package-level convenience functions: `Info()`, `Error()`, `Debug()`, `Warn()`, `GetLogger()`. Create `logger_test.go` with table-driven tests covering both environments, invalid environment handling, and the convenience functions. Target >90% coverage.

## Acceptance Criteria

