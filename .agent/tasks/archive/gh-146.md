# GH-146

**Created:** 2026-04-03

## Problem

separate Gin engine with only correlation ID + recovery middleware (no auth, no rate limiting), registers all `/admin/users`, `/admin/clients`, `/admin/tokens/introspect` routes plus health/metrics. Handler files: `admin_user_handlers.go` (7 CRUD + lock/unlock endpoints), `admin_client_handlers.go` (6 CRUD + rotate-secret endpoints), `admin_token_handlers.go` (introspect endpoint). Wire admin server in `cmd/server/main.go` to start concurrently on port 4001 alongside public server on port 4000, with graceful shutdown. Include comprehensive handler-level tests with mocked services covering success paths, validation errors, not-found, conflicts, and edge cases.

## Acceptance Criteria

