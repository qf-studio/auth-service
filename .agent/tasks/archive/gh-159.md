# GH-159

**Created:** 2026-04-03

## Problem

Create `Makefile` with targets: `build` (go build), `run` (go run cmd/server), `test` (go test -race ./...), `test-cover` (coverage report), `lint` (golangci-lint), `fmt` (go fmt + goimports), `migrate-up`/`migrate-down`, `docker-up`/`docker-down` (docker-compose lifecycle), `clean`.

## Acceptance Criteria

