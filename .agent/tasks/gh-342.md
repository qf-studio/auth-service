# GH-342

**Created:** 2026-04-06

## Problem

Add public SAML HTTP handlers in `internal/api/saml_handlers.go`: `GET /auth/saml/metadata` (SP metadata endpoint), `GET /auth/saml/login` (initiate SSO, redirect to IdP), `POST /auth/saml/acs` (Assertion Consumer Service callback, processes SAML response and issues JWT token pair). Add admin handlers in `internal/api/admin_saml_handlers.go`: full CRUD for IdP configurations at `/admin/saml/idps` (list, get, create, update, delete), `POST /admin/saml/idps/:id/metadata` for IdP metadata XML import, `GET /admin/saml/idps/:id/metadata` for metadata export, and attribute mapping configuration endpoints. Register routes in `router.go` and `admin_router.go`. Wire the SAML service with all dependencies in `cmd/server/main.go`. Add audit events for SAML login, JIT provisioning, and IdP config changes. All with handler tests.

## Acceptance Criteria

