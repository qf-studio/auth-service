# GH-65

**Created:** 2026-04-02

## Problem

Install `testcontainers-go` dependency. Create the full `internal/testutil/` package: `postgres.go` (start PostgreSQL container, run migrations via `golang-migrate`, return `*pgxpool.Pool`), `redis.go` (start Redis container, return `*redis.Client`), `cleanup.go` (truncate tables between tests for isolation), `fixtures.go` (helpers like `CreateTestUser`, `CreateTestClient`, `CreateTestTokenPair`), and `assertions.go` (`AssertErrorCode` and similar response validators). All files gated with `//go:build integration` build tag. Include package-level self-tests (`*_test.go`) that verify containers start, migrations run, fixtures create data, and cleanup works. Container reuse via `TestMain` pattern. Note: fixture helpers may initially be stubs or minimal if domain types from upstream issues (GH-5, GH-7) aren't merged yet — design them against the interfaces in `internal/api/services.go` (`UserInfo`, `AuthResult`).

## Acceptance Criteria

