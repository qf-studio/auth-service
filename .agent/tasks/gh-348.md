# GH-348

**Created:** 2026-04-07

## Problem

GitHub Issue #348: Storage Layer: Tenant-Scoped Repositories

Parent: GH-28

Update all 18 repository implementations in `internal/storage/` to include `tenant_id` in every query (SELECT, INSERT, UPDATE, DELETE). Every `List`, `FindByID`, `Create`, `Update`, `Delete` method must accept tenant ID from context or parameter and scope queries with `WHERE tenant_id = $N`. Update repository interfaces to thread tenant context. Add a `TenantRepository` for CRUD on the tenants table itself. Update `internal/storage/mocks/` to match new interfaces. Update all table-driven tests in this package. This is the largest subtask — all files in `internal/storage/` are one package and must be modified together. Touches: `internal/storage/`.

## Acceptance Criteria

