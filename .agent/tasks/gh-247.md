# GH-247

**Created:** 2026-04-05

## Problem

Add `go-webauthn/webauthn` dependency to `go.mod`. Implement the WebAuthn service in `internal/mfa/webauthn.go`: configure the `webauthn.WebAuthn` relying party, implement the `webauthn.User` interface adapter, and build the four core flows — `BeginRegistration`, `FinishRegistration`, `BeginLogin`, `FinishLogin`. Wire credential lookup/persistence through the storage interfaces from subtask 1. Handle sign-count verification for clone detection. Include unit tests with mock authenticator covering: registration round-trip, authentication round-trip, multiple credentials per user, sign-count validation, and error cases.

## Acceptance Criteria

