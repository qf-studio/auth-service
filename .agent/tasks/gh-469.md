# GH-469

**Created:** 2026-07-26

## Problem

In the `internal/oidc/` package, implement all three interfaces declared in `internal/api/services.go:140-277` against the contracts already fixed by handlers in `internal/api/oidc_handlers.go` and `internal/api/admin_consent_handlers.go`. Includes: authorize with client + exact-match redirect_uri validation, S256-only PKCE enforcement (required for `client_type=public`, rejected `plain`, unregistered redirect_uri returns error page not a redirect); code exchange with one-time consumption, PKCE verifier check via `internal/oauth/pkce.S256Challenge`, confidential-client secret verification, `IssueTokenPair` + new `IssueIDToken` calls; discovery document generated from `OIDCConfig` + route constants; userinfo with bearer validation and scope-gated claim projection; login/consent request GET+PUT with remembered consent persisted to `consent_grants`; auto-consent for first-party client types; and `ApproveClient`. Introduce config `OIDC_LOGIN_UI_URL` (and consent UI URL if distinct). Comprehensive unit tests for each service; do NOT touch the routed handlers or add routes for the unrouted `CreateThirdPartyClient`.

## Acceptance Criteria

