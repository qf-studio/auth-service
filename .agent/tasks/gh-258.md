# GH-258

**Created:** 2026-04-05

## Problem

Add OAuth-specific domain types to `internal/domain/` (e.g., `OAuthAccount` struct, `OAuthProvider` enum, `OAuthUser` intermediate type, request/response DTOs for OAuth endpoints) and create migration `migrations/000008_create_oauth_accounts_table.up.sql` with the `oauth_accounts` table (user_id FK, provider, provider_user_id, email, created_at; unique constraint on provider+provider_user_id). Also add any new sentinel errors to `internal/domain/errors.go`.

## Acceptance Criteria

