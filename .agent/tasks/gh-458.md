# GH-458

**Created:** 2026-07-26

## Problem

Add a "Running migrations from the image" section to deployments/README.md covering the --entrypoint /auth-migrate invocation, required env (DATABASE_URL or POSTGRES_HOST/DB/USER/PASSWORD), the supported subcommands (up, down, version, force), and a dirty-state recovery example. Then post a comment on qf-studio/auth-service#435 with the consumer migration path so Pointer can drop its vendored test/fixtures/auth-migrations/ and the checkout step in deploy-pointer-aws.yml (explicitly note that switching Pointer's workflow is their follow-up — do not touch it here).

## Acceptance Criteria

