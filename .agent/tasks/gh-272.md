# GH-272

**Created:** 2026-04-06

## Problem

GitHub Issue #272: Database migrations, domain models, and storage layer

Parent: GH-27

Foundation for all OIDC provider functionality. Create migration `000008` adding `authorization_codes` table (code hash, client_id, user_id, redirect_uri, scopes, code_challenge, code_challenge_method, nonce, expires_at, used_at) and `consent_sessions` table (challenge, verifier, client_id, user_id, requested_scopes, granted_scopes, state enum, login_session_id, encrypted payload). Also migration `000009` to add `redirect_uris`, `is_third_party`, `approval_status` columns to the existing `clients` table. Define corresponding domain types in `internal/domain/` (AuthorizationCode, ConsentSession, ConsentState enum, IDTokenClaims, UserInfoResponse; extend Client struct with new fields). Create repository interfaces in `internal/storage/` (AuthorizationCodeRepository, ConsentSessionRepository) and their PostgreSQL implementations. Touches: `migrations/`, `internal/domain/`, `internal/storage/`.

## Acceptance Criteria

